#pragma once

#include <chrono>
#include <utility>
#include <string>
#include <vector>

#define HLINE "-------------------------------------------------------------\n"

int d_main(const std::string &node, const std::string &socket, std::vector<double> &time, std::vector<double> &energy);