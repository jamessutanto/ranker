#pragma once

#include <chrono>
#include <utility>
#include <string>

#define HLINE "-------------------------------------------------------------\n"

std::pair<double,double> d_main(const std::string &node, const std::string &socket);